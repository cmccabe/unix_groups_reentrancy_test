unix-groups-reentrancy-test
======================
Test whether the UNIX users and groups APIs on a system are re-entrant by
performing many operations in parallel.

How do I build the source code?
----------------------------------
    ./configure
    make
    sudo make install
